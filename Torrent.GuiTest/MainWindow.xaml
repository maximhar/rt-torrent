<Window x:Class="Torrent.GuiTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Torrent.GuiTest"
        Title="MainWindow" Height="350" Width="525" AllowDrop="True" Drop="Window_Drop">
    <Grid Margin="3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*" MinWidth="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Column="0">Torrent path:</Label>
        <TextBox Grid.Column="1" Name="AnnounceUrlTextBox"
                 AllowDrop="True"/>
        <Label Grid.Row="1" Grid.Column="0">Torrent hash:</Label>
        <TextBox Name="HashTextBox" Grid.Row="1" Grid.Column="1" IsEnabled="False" Text="{Binding Hash}"/>
        <ListBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" ItemsSource="{Binding Peers}" >
            
        </ListBox>
        <Button Name="GetPeersButton" Grid.Row="3" Grid.ColumnSpan="2" Click="GetPeersButton_Click">Get peers from tracker</Button>
    </Grid>
</Window>
