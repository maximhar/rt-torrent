<Window x:Class="Torrent.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Torrent.Gui"
        Title="MainWindow" Height="350" Width="525">
    <Grid AllowDrop="True" Drop="Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Left">
            <Button Command="{Binding AddTransferCommand}">Add...</Button>
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding Transfers}" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="local:Transfer">
                    <Border BorderThickness="2" BorderBrush="Black">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Row="0" Content="{Binding Name}"/>
                            <Label Grid.Row="1" Content="{Binding Mode}"/>
                            <StackPanel Orientation="Vertical" VerticalAlignment="Stretch" Grid.Column="1" Grid.RowSpan="2">
                                <Button Command="{Binding StartCommand}">Start</Button>
                                <Button Command="{Binding StopCommand}">Stop</Button>
                            </StackPanel>
                            <ProgressBar Maximum="1000" Height="12" Grid.Row="2" Value="{Binding Progress}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
